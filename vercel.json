{
  "buildCommand": "npm run build",
  "env": [
    "POSTGRES_URL",
    "DATABASE_URL",
    "JWT_SECRET"
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "regions": ["sfo1"]
}
